#!/bin/bash
# Begin LSF directives
#BSUB -P gen150
#BSUB -J CAE
#BSUB -o caeML.txt
#BSUB -W 0:30
#BSUB -nnodes 1
# End LSF directives and begin shell commands

# source summit_env.sh
module load open-ce
NODES=$(cat ${LSB_DJOB_HOSTFILE} | sort | uniq | grep -v login | grep -v batch | wc -l)

jsrun -n${NODES} -a 1 -g 1 -c1 -r1  python cae_ml.py